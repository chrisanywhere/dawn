// Custom javscript for Spacemaker Wardrobes
// ++++++++++++++++++++++++++++++++++++++++++

// Document Ready
//$(function() {
    
  // Global Variables
  //var $globalNavbar = $('#globalNavbar');
  //var $globalContent = $('#globalContent');
  //var $globalFooter = $('#globalFooter');

  // Enabling Popover Example 1 - HTML (content and title from html tags of element)
  // $('body').popover({
  //   selector: '[data-toggle=popover]'
  // });

  // Enabling Tooltips Example 1 - HTML (content and title from html tags of element)
  // $('body').tooltip({
  //   selector: '[data-toggle=tooltip]'
  // });

  // Basic Spinner
  //var spinner = new Spinner().spin();

  // Consignment Tracking Functionality
  // Currently commented out as not in use
  /**
  var copeLink = "http://tracking.cope.com.au/track.php?consignment=";
  var deliveryTracker = '#deliveryTracker';
  var consignmentField = '#consignmentField';
  var copeiFrame = 'iframe#copeiFrame';
  var conNumber;
  $(deliveryTracker).on('show.bs.modal', function (event) {
    var conNumber = $(consignmentField).val();
    $(copeiFrame).attr("src",copeLink + conNumber);
  });
  */
 
  // initiate fancybox
  //$(".fancybox").fancybox();

//});