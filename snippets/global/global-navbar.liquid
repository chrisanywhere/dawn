<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" id="globalNavbar">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav">
        <span class="sr-only">Toggle main navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      
      <a class="navbar-brand" href="/" title="Spacemaker Wardrobes - Home"><span class="space">Space</span><strong>maker</strong></a>
    </div>

    <div class="collapse navbar-collapse" id="main-nav">
      <ul class="nav navbar-nav">
        {% for l in linklists.main-menu.links %}
        {% include '_navbar-link' with 0 %}
        {% endfor %}
      </ul>
      
      <div class="hidden-xs">
        <ul class="nav navbar-nav navbar-right">
          {% capture items %}items{% endcapture %}
          {% capture item %}item{% endcapture %}
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon icon-shopping-cart"></i></a>
            <ul class="dropdown-menu">
              <li><a href="/cart" title="View Cart"><strong>View Cart</strong> <span class="badge">{{ cart.item_count }} {{ cart.item_count | pluralize: item, items }}</span></a></li>
              {% if cart.item_count > 0 %}
              <li><a href="/checkout" title="Checkout">Checkout</a></li>
              {% endif %}
            </ul>
          </li>

          {% if shop.customer_accounts_enabled %}
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon icon-user"></i></a>
            <ul class="dropdown-menu">
              {% if customer %}
              <li><a href="/account">Welcome, {{ customer.first_name }}!</a></li>
              <li>{% capture logout %}Log Out{% endcapture %}{{ logout | customer_logout_link }}</li>
              {% else %}
              <li>{% capture register %}Register{% endcapture %}{{ register | customer_register_link }}</li>
              <li>{% capture login %}Log In{% endcapture %}{{ login | customer_login_link }}</li>
              {% endif %}
            </ul>
          </li>
          {% endif %}
        </ul>
      </div>

      <div class="visible-xs">
        <ul class="nav navbar-nav">
          <li><a href="/cart">View Cart <span class="badge">{{ cart.item_count }} {{ cart.item_count | pluralize: item, items }}</span></a></li>
          {% if cart.item_count > 0 %}<li><a href="/checkout" title="Checkout">Checkout</a></li>{% endif %}
          {% if shop.customer_accounts_enabled %}
            {% if customer %}
            <li><a href="/account">Welcome, {{ customer.first_name }}!</a></li>
            <li>{% capture logout %}Log Out{% endcapture %}{{ logout | customer_logout_link }}</li>
            {% else %}
            <li>{% capture register %}Register{% endcapture %}{{ register | customer_register_link }}</li>
            <li>{% capture login %}Log In{% endcapture %}{{ login | customer_login_link }}</li>
            {% endif %}
          {% endif %}
        </ul>
      </div>
    </div>

  </div>
</nav>